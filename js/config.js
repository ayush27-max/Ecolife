window.ECOLIFE_CONFIG = {
    // For local testing you can keep using localStorage key "gemini_api_key".
    // For deployment, replace this placeholder with your Gemini API key.
    // Note: browser-side keys are visible to users; restrict this key in Google AI Studio.
    geminiApiKey: 'YOUR_GEMINI_API_KEY',
    geminiModel: 'gemini-3.5-flash-lite'
};